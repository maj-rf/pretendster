// import supertest from 'supertest';
// import app from '../app';

// const api = supertest(app);

// describe('User routes', () => {
//   const baseURL = '/api/v1/users';

//   test('[GET] all users', async () => {
//     await api.get(`${baseURL}/all`).expect(200);
//   });
// });
